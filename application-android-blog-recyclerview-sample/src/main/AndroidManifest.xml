<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yibaoshan"
        tools:replace="android:appComponentFactory">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Yibaoshan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--基本的使用方式示例-->
        <activity android:name=".ui.basic.BasicExampleOfUsingRecyclerViewGuideActivity" />
        <activity android:name=".ui.basic.BasicExampleOfUsingRecyclerViewDetailActivity" />

        <!--设置 item 的点击与长按事件-->
        <activity android:name=".ui.click.ItemClicksGuideExampleActivity" />
        <activity android:name=".ui.click.ItemClicksBaseClickListenerActivity" />
        <activity android:name=".ui.click.ItemClicksBaseItemTouchListenerActivity" />

    </application>

</manifest>