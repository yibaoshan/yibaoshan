package com.android.notebook.backup.android.docs

class AndroidVersion {

    /**
     *
     * Android API Level对比
     *
     * Android4.4 -> 19 -> Android K
     * 1. 支持NFC
     * 2. 支持沉浸式状态栏
     *
     * Android4.4w -> 20 -> Android K
     *
     * Android5.0 -> 21 -> Android L
     * 1. Material Design
     * 2. 支持64位处理器
     * 3. Dalvik虚拟机转为ART虚拟机
     *
     * Android5.1 -> 22 -> Android L
     *
     * Android6.0 -> 23 -> Android M
     * 1. 新增运行时权限
     * 2. 原生系统支持指纹识别功能
     *
     * Android7.0 -> 24 -> Android N
     * 1. 支持应用分屏(多窗口)
     * 2. 加入暗色主题
     *
     * Android7.1 -> 25 -> Android N
     *
     * Android8.0 -> 26 -> Android O
     * 1. 支持画中画模式
     * 2. Project Treble模块化架构
     *
     * Android8.1 -> 27 -> Android O
     *
     * Android9.0 -> 28 -> Android P
     * 1. 适配刘海屏
     *
     * Android10.0 -> 29
     * 1. 支持暗黑模式
     * 2. 支持5G
     * 3. 支持手势导航
     *
     * Android11.0 -> 30
     *
     * 详情请查看：https://zh.wikipedia.org/wiki/Android%E7%89%88%E6%9C%AC%E5%88%97%E8%A1%A8#
     *
     * */

    /**
     * support.v4
     * 其中，4代表Android api版本号，对应系统版本号为1.6
     * v4包最低可以支持到1.6的版本
     *
     * support.v7
     * api为7的系统版本为2.1
     *
     * Android x 是在support包基础上做的一次升级，整改，大一统
     *
     * */

}