### 1、为什么Java只有值传递？

Java方法的形参传递的不管是基础数据类型还是引用数据类型，都是值传递
为什么这么说呢？
当传递的是基础数据类型时，在方法内部更改基础数据类型的值，外部的值显然不会改变
当传递的是引用数据类型时，更改引用数据类型内部值，外部也会跟着一起变，这是因为传递过来的是外部的引用地址；
但是，当我们把新的数据赋值给形参时，就会发现外部的没有改变了，因为外部引用地址已经被更改了